# Ralph Progress Log
Started: 2026-07-01

## Codebase Patterns
- Build with `npm run build` (tsc), typecheck with `npm run typecheck` (tsc --noEmit)
- Entry point is `src/index.ts` with shebang `#!/usr/bin/env node`
- Compiled output goes to `dist/` directory
- Uses Node16 module resolution (ESM-compatible with .js extensions in imports)
- @types/node is needed for `process` global; added as devDependency
- No external runtime dependencies; uses built-in `fetch` for HTTP
- Arg parsing lives in `parseArgs`; flags checked against a `known` Set, unknown flags exit non-zero
- Mutually-exclusive flags (e.g. --beta/--next) are validated in `parseArgs` and exit 1 on conflict
- npm version lookups hit `https://registry.npmjs.org/n8n/{tag}`
- n8n GitHub release tags use format `n8n@{version}` via `/repos/n8n-io/n8n/releases/tags/{tag}`
- Responses are validated for shape before use; errors thrown with status/context
- `main().catch` prints `Error: {message}` to stderr and exits 1
---

## [2026-07-01] - add-helm-chart-version - 1. Argument parsing
- Added `--helm` to the `known` set in `parseArgs`
- Extended `ParsedArgs` interface with `helm: boolean`
- Added conflict check: `--helm` with `--beta` or `--next` exits 1 with error message
- Updated usage string to include `[--helm]`
- Files changed: `src/index.ts`
- **Learnings for future iterations:**
  - Conflict checks follow the same pattern as the existing `--beta`/`--next` guard
  - Build + typecheck pass cleanly; no new deps needed
- `main()` helm branch: early return after handling helm prevents fallthrough to npm path
---

## [2026-07-01] - add-helm-chart-version - 2. Helm chart fetch
- Added `fetchHelmChartRelease()` that hits `/repos/n8n-io/n8n-hosting/releases/latest`
- Validates response shape (tag_name + body), throws with status on non-success
- Strips leading `v` from tag_name to produce clean version string
- Files changed: `src/index.ts`
- **Learnings for future iterations:**
  - GitHub latest-release endpoint: `/repos/{owner}/{repo}/releases/latest`
  - n8n-hosting tags use `v`-prefixed semver (e.g. `v1.10.1`); strip with `.startsWith("v") ? .slice(1) : tag`
  - Pattern matches the existing `fetchChangelog` function structure
---

## [2026-07-01] - add-helm-chart-version - 3. Wire into main
- Branched on `helm` flag in `main()` to call `fetchHelmChartRelease()`
- Helm path: prints `release.version` by default; prints `release.tagName` + notes when `--changelog`
- Early `return` after helm branch preserves existing npm path unchanged
- Files changed: `src/index.ts`
- **Learnings for future iterations:**
  - `--helm --changelog` prints `tagName` (with leading `v`) to match the n8n changelog output style
  - Live smoke test confirmed: `--helm` → `1.10.1`, `--helm --changelog` → tag + notes, `--helm --beta` → exit 1
---

## [2026-07-01] - add-helm-chart-version - 4. Verification
- Ran live smoke tests: `--helm` → `1.10.1` (exit 0), `--helm --changelog` → tag + release notes (exit 0), `--helm --beta` → error (exit 1)
- Added Phase 2 section to PRD.md documenting the `--helm` flag, `--changelog` combination, and conflict rules
- `openspec validate add-helm-chart-version --strict` passes
- Files changed: `PRD.md`, `openspec/changes/add-helm-chart-version/tasks.md`
- **Learnings for future iterations:**
  - Verification task sections are quick when preceding sections left the impl complete; just run the scenarios and document in PRD
---

## [2026-07-01] - add-terraform-module-version - 1. Argument parsing
- Added `--terraform` to the `known` set in `parseArgs`
- Extended `ParsedArgs` interface with `terraform: boolean`
- Added conflict check: `--terraform` with `--helm`, `--beta`, or `--next` exits 1 with error message
- Updated usage string to include `[--terraform]`
- Destructured `terraform` in `main()` to avoid unused-variable type error
- Files changed: `src/index.ts`
- **Learnings for future iterations:**
  - New flag pattern identical to `--helm` addition; extend `known`, add interface field, add conflict guard, update usage string, destructure in main
---

## [2026-07-01] - add-terraform-module-version - 2. Terraform module fetch
- Added `fetchTerraformModuleVersion()` that hits `https://registry.terraform.io/v1/modules/n8n-io/n8n/aws`
- Validates response shape (presence and type of `version`), throws with status on non-success
- Added `fetchTerraformChangelog(version)` that fetches the `n8n-io/terraform-aws-n8n` GitHub release by tag (no `v` prefix)
- Files changed: `src/index.ts`, `openspec/changes/add-terraform-module-version/tasks.md`
- **Learnings for future iterations:**
  - Terraform Registry base URL: `https://registry.terraform.io`; version is returned verbatim (no `v` prefix to strip)
  - terraform-aws-n8n release tags use no `v` prefix (e.g. `0.1.0`), unlike n8n-hosting which uses `v`-prefixed tags
  - Pattern is identical to `fetchHelmChartRelease` / `fetchChangelog`; just different URLs and no prefix stripping
---

## [2026-07-01] - add-terraform-module-version - 3. Wire into main
- Branched on `terraform` flag in `main()` before the `helm` branch
- `--terraform` calls `fetchTerraformModuleVersion()` and prints version; `--terraform --changelog` also calls `fetchTerraformChangelog()` and prints tag + notes
- Early `return` preserves existing npm and helm paths unchanged
- Files changed: `src/index.ts`, `openspec/changes/add-terraform-module-version/tasks.md`
- **Learnings for future iterations:**
  - terraform branch goes before helm branch; early return pattern keeps paths independent
  - Live smoke tests: `--terraform` → `0.1.0` (exit 0), `--terraform --changelog` → tag + notes (exit 0), `--terraform --helm` → exit 1, `--terraform --beta` → exit 1
---

## [2026-07-01] - add-terraform-module-version - 4. README
- Added `README.md` with install section and usage for all flags: default npm, `--beta`, `--next`, `--changelog`, `--helm`, `--terraform`
- Updated `PRD.md` to add Phase 3 documenting the `--terraform` flag with acceptance criteria and considerations
- Files changed: `README.md` (new), `PRD.md`
- **Learnings for future iterations:**
  - README flags table is a useful quick-reference pattern for CLI tools
  - PRD phases follow the same structure: hypothesis/requirement/acceptance criteria/considerations
---

## [2026-07-01] - add-terraform-module-version - 5. Verification
- Smoke-tested all three scenarios: `--terraform` → `0.1.0` (exit 0), `--terraform --changelog` → tag + release notes (exit 0), `--terraform --helm` → error (exit 1)
- `npm run typecheck` clean; `openspec validate add-terraform-module-version --strict` passes
- Files changed: `openspec/changes/add-terraform-module-version/tasks.md`
- **Learnings for future iterations:**
  - Verification section is straightforward when preceding sections completed the impl; run live smoke tests, validate, check off tasks
---

## [2026-07-01] - harden-cli - 1. Shared network helpers
- Added `USER_AGENT = "n8nrel/1.0.0"` constant used on every request
- Added `fetchJson(url, extraHeaders?)` with User-Agent, AbortSignal.timeout(10_000), ok-check, returns parsed JSON
- Added `fetchGitHubRelease(url)` on top of fetchJson with GitHub Accept header and shape validation
- Rewrote all five fetch functions (fetchVersion, fetchChangelog, fetchHelmChartRelease, fetchTerraformModuleVersion, fetchTerraformChangelog) to delegate to the shared helpers
- Timeout aborts are propagated through main().catch as "Error: This operation was aborted" (built-in AbortError message)
- Files changed: `src/index.ts`
- **Learnings for future iterations:**
  - `AbortSignal.timeout(ms)` is available in Node 17.3+ and works with fetch; no polyfill needed
  - `fetchGitHubRelease` wraps `fetchJson` with Accept header and shape validation; all GitHub calls go through it
  - `fetchJson` error uses `new URL(url).hostname` for the host in the message (e.g. "api.github.com returned 404 Not Found")
  - Keep wrapper functions thin - the original per-function error specificity is less important than a clean abstraction
---

## [2026-07-01] - harden-cli - 2. Argument validation and help
- Added `USAGE` constant (replaces inline usage string duplicated across error messages)
- Added `--help`/`-h` handling at the top of `parseArgs` before any other validation: prints usage to stdout, exits 0; `--help` takes precedence over other flags
- Added positional-argument rejection (tokens not starting with `-`): stderr + exit 1
- Added `--flag=value` form rejection (tokens containing `=`): stderr + exit 1
- Existing conflict rules and unknown-flag check unchanged; updated usage string printed there too
- Files changed: `src/index.ts`, `openspec/changes/harden-cli/tasks.md`
- **Learnings for future iterations:**
  - `--help`/`-h` check must be the very first thing in `parseArgs` (before positional/equals checks)
  - Positional check: `args.filter(a => !a.startsWith("-"))` — catches bare words; `-h` passes because it starts with `-`
  - Equals check: `args.filter(a => a.includes("="))` — catches `--flag=value` before the known-set lookup
  - Factoring `USAGE` into a const avoids string drift across four separate error paths
---

## [2026-07-01] - harden-cli - 3. Changelog printing helper
- Added `printRelease({ tagName, body })` function above `main`
- Replaced all three identical changelog-print blocks (terraform, helm, default) with calls to the helper
- Files changed: `src/index.ts`, `openspec/changes/harden-cli/tasks.md`
- **Learnings for future iterations:**
  - The three branches had identical `console.log(release.tagName); if (release.body)` patterns; helper reduces to one place
  - TypeScript infers the parameter type from the destructured shape; explicit `{ tagName: string; body: string }` annotation needed since the helper is standalone
---

## [2026-07-01] - harden-cli - 4. Tests
- Exported `parseArgs` from `src/index.ts`; guarded `main()` call with `require.main === module` to prevent execution on import
- Added `src/index.test.ts` with 19 `node:test` + `node:assert` unit tests covering: default tag, --beta, --next, --changelog, --helm, --terraform, all conflict rules, --help/-h (exit 0 + stdout), --flag=value rejection, positional-arg rejection, unknown-flag rejection
- Added `"test": "node --test dist/index.test.js"` script to `package.json`
- Confirmed `init.sh` test detection works: test script is non-placeholder so init.sh runs it
- Files changed: `src/index.ts`, `src/index.test.ts`, `package.json`, `openspec/changes/harden-cli/tasks.md`
- **Learnings for future iterations:**
  - `require.main === module` is the CJS guard for "only run when invoked directly" - works because tsconfig module=Node16 + no "type":"module" outputs CJS
  - `t.mock.method(obj, 'methodName', fn)` in `node:test` auto-restores after each test; throw inside the mock to simulate process.exit
  - Test the compiled dist output directly with `node --test dist/index.test.js`; no transpile-on-the-fly needed
  - `mock.method` returns a MockFunctionContext; mocking process.exit requires throwing to stop execution since TypeScript types it as `never`
---

## [2026-07-01] - harden-cli - 5. Docs and verification
- Added `--help`/`-h` to usage line and flags table in README.md
- Ran `npm run typecheck`, `npm run build`, `npm test` — all 19 tests pass
- Live smoke tests confirmed: `--changelog` (exit 0), `--helm` (exit 0), `--terraform --changelog` (exit 0), `--help` prints usage (exit 0)
- `openspec validate harden-cli --strict` passes
- Files changed: `README.md`, `openspec/changes/harden-cli/tasks.md`
- **Learnings for future iterations:**
  - Docs-only sections are fast: update README, run checks, validate, commit
  - Live smoke tests for network-dependent flags confirm User-Agent header is working (no 403s)
---

## [2026-07-10] - add-all-versions-table - 1. Argument parsing
- Added `all: boolean` to `ParsedArgs`, `--all` to the `known` flags set, and destructured `all` in `parseArgs`
- Added mutual-exclusion guard: `--all` combined with `--beta`, `--next`, `--helm`, `--terraform`, or `--changelog` exits 1 with an error message
- Updated `USAGE` string to include `[--all]`
- Added parser unit tests: `--all` alone sets `all: true`; each of the five mutual-exclusion combinations (`--all --beta`, `--all --next`, `--all --helm`, `--all --terraform`, `--all --changelog`) exits 1 with non-empty stderr
- Updated the existing "no flags" deepEqual test to include the new `all: false` field
- Files changed: `src/index.ts`, `src/index.test.ts`, `openspec/changes/add-all-versions-table/tasks.md`
- **Learnings for future iterations:**
  - New flag pattern is now well-established: add to `ParsedArgs`, add to `known` Set, destructure + conflict-check in `parseArgs`, update `USAGE`, add tests. `--all`'s conflict check lists all five other flags in one guard since it excludes everything.
  - `main()` still only destructures `{ tag, changelog, helm, terraform }` from `parseArgs` result; dispatch logic for `all` is deferred to task 3 (aggregate lookup + table not implemented yet)
  - Build, typecheck, and all 25 tests pass after this section
---

## [2026-07-10] - add-all-versions-table - 2. Aggregate lookup and table
- Added `fetchAllVersions()`: runs `fetchVersion("latest"|"beta"|"next")`,
  `fetchHelmChartRelease().then(r => r.version)`, and
  `fetchTerraformModuleVersion()` via `Promise.allSettled`; maps rejected
  results to `"n/a"`
- Added `formatVersionTable(versions)`: renders stable/beta/next/helm/terraform
  rows, label column padded to the longest label + 2 spaces so all version
  values start at the same column
- Exported `fetchAllVersions` and `formatVersionTable` alongside `parseArgs` for
  unit testing
- Added two unit tests: all-resolved values, and a mix of resolved + `n/a`;
  both assert row order and that all version values start at the same column
- Files changed: `src/index.ts`, `src/index.test.ts`
- **Learnings for future iterations:**
  - `Promise.allSettled` + a small `resolve()` helper (`status === "fulfilled" ? value : "n/a"`) is the clean pattern for "run N lookups, don't let one failure kill the rest"
  - `main()` is not wired to `all` yet (that's section 3); `all` is destructured but unused in `main` until then, which is fine, no lint script exists to flag it
  - Column alignment: `label.padEnd(maxLabelLength + 2)` is simplest; test by regex-matching the whitespace immediately before the trailing version token and asserting all rows have the same start index
