{
  "name": "n8nrel",
  "version": "1.0.0",
  "description": "A lightweight CLI tool that fetches and prints the latest stable n8n release version from the npm registry.",
  "bin": {
    "n8nrel": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test dist/index.test.js"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.7.0"
  }
}
